program begin

	com firstVar: i32 = 5;
	com secondVar: i32 = 10;

	if COMPARE.IsLessThan(firstVar, secondVar) then
		add firstVar, 200;
		add secondVar, 20;
	end;

	if COMPARE.IsMoreThan(firstVar, secondVar) then
		add secondVar, 20;
	end;

	llreturn secondVar;

end