program begin

	com firstVar: i32 = 5;
	com secondVar: i32 = 10;

	if COMPARE.IsLessThan(firstVar, secondVar) then
		add firstVar, 200;
		add secondVar, 20;
	end;

	if COMPARE.IsLessThan(firstVar, 100) then
		add secondVar, 20;
	else if COMPARE.IsLessThan(firstVar, 190) then
		add secondVar, 2000;
	else then
		add secondVar, 1000;
	end;

	llreturn secondVar;

end